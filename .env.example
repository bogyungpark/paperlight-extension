# Paperlight environment example.
# Runtime configuration lives in chrome.storage.local (see Options page).
# These VITE_ entries are only consulted for static defaults at build time.

VITE_DEFAULT_BASE_URL=http://192.168.110.106:8001/v1
VITE_DEFAULT_MODEL=Qwen/Qwen2.5-32B-Instruct-AWQ
